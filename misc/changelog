(kernel changes)
(newest>oldest)

 - (kernel/user) Refactored EXNIX to use directories in source
 - (kernel/user) prototype kernel scr node done, shapetest debug program
 - (user) MAN utility added
 - (kernel) bump version to RC4 (0.01-4)
 - (kernel/user) console tflags work (stty syscall)
 - (user) fixed PWD failing on nested directories
 - (kernel/user) finished font8x16, added character test to debugger
 - (user/kernel) extensively fixed compilation warnings (gcc version 12.2.0)
 - (user) added MKNOD user program
 - (user) revise INIT program
 - (user) added MORE user program
 - (kernel) added /dev/random device node (and standard devinit func)
 - (user) in progress cleaning up warnings
 - (kernel) version name displayed on boot (defined version in ../include/utsname.h)
 - (kernel) cat graphic mapped to 0x01 - 0x02 in font8x16.c
 - (kernel) console colour changed to Amber
 - (kernel) filewrite fixed to write mem to disk (fixes touch/date)

(todo)
 - (kernel) UID permissions and modes
 - (kernel) LS mode listing fix
 - (kernel) graphical device node
 - (user) better INIT program
 - (user) MAN is nearly done, but still incomplete. 
 - (user) port V7 crypt to Userspace

!!!!  FIX MAN  !!!!!!!!!!!!!

